<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/notes_fg">

    <fragment
        android:id="@+id/notes_fg"
        android:name="com.example.smartnotebook.notes_fg"
        android:label="fragment_notes_fg"
        tools:layout="@layout/fragment_notes_fg" >
        <action
            android:id="@+id/action_notes_fg_to_createNote"
            app:destination="@id/createNote" />
    </fragment>
    <activity
        android:id="@+id/createNote"
        android:name="com.example.smartnotebook.CreateNote"
        android:label="activity_create_note"
        tools:layout="@layout/activity_create_note" />
</navigation>